#include "graph.hpp"
#include <vector>

Graph find_vertex_cover(Graph *g, std::vector<Edge> edge_list){
    for(int i = 0; i < edge_list.size(); ++i){
        
    }
}

void check_vertex_cover(Graph *g, Graph *vertex_cover){

}
